import React from 'react';

export function PersonalDetails(props: { prevStep: () => void, handleChange: (input: any) => (e: { target: { value: any } }) => void, nextStep: () => void, values: {} }) {
    return <div>Personal Details</div>;
}